name: Publish to PyPI

# Triggered by pushing a version tag: git tag v0.3.0 && git push --tags
#
# SETUP (one-time):
# 1. PyPI: pypi.org/manage/project/pixeltable-new/settings/publishing/
#    Add trusted publisher: owner=pixeltable, repo=pixeltable-new,
#    workflow=publish.yml, environment=pypi
# 2. GitHub: github.com/pixeltable/pixeltable-new/settings/environments
#    Create environment named "pypi"

on:
  push:
    tags:
      - "v*"

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.13"]
      fail-fast: true
    runs-on: ${{ matrix.os }}
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: pyproject.toml
      - run: uv sync
      - run: uv run ruff check src/ tests/
      - run: uv run ruff format src/ tests/ --check
      - run: uv run python -m pytest tests/ -v

  publish:
    needs: test
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v5
      - run: uv build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
